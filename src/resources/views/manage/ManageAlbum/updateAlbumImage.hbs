<a href="/manage/Album/{{album._id}}">Sửa album</a>
<a href="/manage/AlbumImage/{{album._id}}">Sửa ảnh album</a>
<h1>Sửa ảnh album</h1>
<h4 style="color:green">{{message}}</h4>
<div class="mt-4 col-lg-6">
    <form method="post" action="updateAlbumImage?_method=PUT" enctype="multipart/form-data" > 
        <input type="hidden" name="idAlbum" value="{{album._id}}">
        <div class="form-group">
            <label for="image">chọn hình cho album</label>
            <input type="file" class="form-control-file" id="image" name="image" accept=".png, .jpg" required>
        </div>
        <h3>Hình hiện tại</h3>
        <img src="{{album.imageAlbum}}" alt="{{albumImageAlt}}" width="300" height="400">

        <br />
        <button type="submit" class="btn btn-primary">sửa hình album</button>
        <a href="/manage/Album" class="btn btn-primary">Trở lại</a>
    </form>
</div>